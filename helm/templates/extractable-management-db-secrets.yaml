{{- $releaseName := .Release.Name -}}
{{- $chartName := include "extractable-management.name" . -}}
{{- $postgres := include "merged.postgres" . | fromYaml }}
{{- if not $postgres.user.useExternal -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $postgres.user.secretName }}
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: {{ $chartName }}
    component: {{ $chartName }}
    release: {{ $releaseName }}
    {{- include "extractable-management.labels" . | nindent 4 }}
type: Opaque
data:
  username: {{ default "defaultuser" $postgres.user.username | b64enc }}
  {{- if $postgres.user.requirePassword }}
  password: {{ default "defaultpassword" $postgres.user.password | b64enc }}
  {{- end }}
{{- end }}
